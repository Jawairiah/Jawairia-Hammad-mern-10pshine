{
  "name": "notes-app-backend",
  "version": "1.0.0",
  "description": "Backend API for Notes Application",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "cross-env NODE_ENV=test mocha \"tests/**/*.test.js\" --timeout 10000 --exit",
    "test:watch": "NODE_ENV=test mocha 'tests/**/*.test.js' --timeout 10000 --watch"
  },
  "keywords": [
    "notes",
    "api",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.7",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "chai-http": "^4.4.0",
    "cross-env": "^10.1.0",
    "mocha": "^10.2.0",
    "nodemon": "^3.0.2"
  }
}
